# Metadata Extraction Module Dependencies
# For extracting structured metadata from research papers in Qdrant

# Core dependencies
pydantic>=2.0.0
python-dotenv>=1.0.0

# Qdrant client
qdrant-client>=1.7.0

# LangChain ecosystem
langchain-core>=0.1.0
langchain-groq>=0.0.1
langgraph>=0.0.30

# Optional (if not already installed in environment)
# typing-extensions>=4.5.0
